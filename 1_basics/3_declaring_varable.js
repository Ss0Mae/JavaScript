/**
 * Variable 선언하기
 * 1) var -- 더이상 쓰지 않음 (너무 구식임)
 * 2) let
 * 3) const
 */
const name = "ssomae";
console.log(name, "Hi");
// const는 항상 값을 할당과 동시에 선언을 해야함
/**
 * let 과 vat로 선언하면
 * 값을 추후에 변경 할 수 있음
 */